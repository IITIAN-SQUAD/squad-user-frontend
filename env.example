# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# Database Configuration (if using database)
DATABASE_URL=your_database_url_here

# Feature Flags - Control which features are visible in production
# Comma-separated list of enabled features
# Available features: blog, practice, analytics, revision, challenge, pyq, coaching, doubts
# Examples:
# - For blog only: NEXT_PUBLIC_ENABLED_FEATURES=blog
# - For blog + practice: NEXT_PUBLIC_ENABLED_FEATURES=blog,practice
# - For all features: NEXT_PUBLIC_ENABLED_FEATURES=blog,practice,analytics,revision,challenge,pyq,coaching,doubts
# - Leave empty or comment out to enable all features by default
NEXT_PUBLIC_ENABLED_FEATURES=blog